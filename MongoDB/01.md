# Create Collection with Schema Validation

```js
db.createCollection("fruits", {
   validator: {
      $jsonSchema: {
         bsonType: "object",
         required: [ "id", "name", "color", "price", "tag" ],
         properties: {
            id: {
               bsonType: "int"
            },
            name: {
               bsonType: "string"
            },
            color: {
               bsonType: "string"
            },
            price: {
               bsonType: "decimal"
            },
            tag: {
               bsonType: "array",
               items: {
                  bsonType: "string"
               }
            }
         }
      }
   }
})
```
