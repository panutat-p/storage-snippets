# Collection

## Schema validation

```js
db.createCollection("fruits", {
   validator: {
      $jsonSchema: {
         bsonType: "object",
         required: [ "name", "color", "price", "tag", "created_at", "updated_at" ],
         properties: {
            name: {
               bsonType: "string"
            },
            color: {
               bsonType: "string"
            },
            price: {
               bsonType: "decimal"
            },
            tag: {
               bsonType: "array",
               items: {
                  bsonType: "string"
               }
            },
            updated_at: {
               bsonType: "date"
            },
            created_at: {
               bsonType: "date"
            }
         }
      }
   }
})
```
